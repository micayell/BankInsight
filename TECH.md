# BankInsight: 핵심 기술 명세 및 아키텍처 (TECH.md)

본 문서는 BankInsight 프로젝트를 구현하며 **어떠한 핵심 기술을 왜(Why) 채택했는지**, 그리고 **애플리케이션의 핵심 비즈니스 로직을 어떻게 설계했는지**에 대한 기술 요약본입니다. 

*(💡 심도 깊은 버그 해결 과정이나 예외 처리 파이프라인 등은 별도의 `TROUBLESHOOTING.md` 문서를 참고해 주시기 바랍니다.)*

---

## 1. 기반 인프라 및 아키텍처 (Infrastructure & Architecture)

### 1-1. Python Django & Django REST Framework (백엔드)
* **Why**: 빠르고 안정적인 MVT 아키텍처 지원과 방대한 금융 데이터를 ORM을 통해 객체 지향적으로 처리하기 위해 Django를 채택했습니다. 또한, Vue.js 프론트엔드와 독립적으로 데이터를 주고받기 위해 DRF를 활용해 완벽하게 분리된 RESTful API 서버를 구축했습니다.
* **How**: 공공데이터(금융감독원)에서 수집하는 대규모 금융상품(예적금, 주담대, 전세자금대출) 데이터를 일괄 `sync`하는 일괄 처리 스크립트를 짜고, `models.JSONField` 및 `ManyToMany` 패턴을 활용해 사용자별 '관심(찜) 상품'을 DB 상에서 다대다 관계로 역참조하도록 설계했습니다.

### 1-2. Vue.js 3 & Vite & Pinia (프론트엔드)
* **Why**: SPA(Single Page Application) 기반의 매끄러운 화면 전환과 Composition API의 직관적인 반응성 상태(Reactivity) 제어를 위해 최신 Vue 3를 적용했으며, 번들링 속도 최적화를 위해 Vite를 결합했습니다.
* **How**: 사용자의 로그인 세션과 '찜한 은행/상품 내역'이 지도 페이지(Map), 프로필 페이지(Profile), 상품 상세 페이지(Detail) 등 애플리케이션 사방에서 동일하게 공유 및 갱신되어야 함을 설계 초기부터 인지하고, 전역 상태 관리자인 **Pinia**를 글로벌 스토어로 도입했습니다.

---

## 2. 모듈 별 비즈니스 로직 구현 설계 (Core Business Logic)

### 2-1. 카카오맵 모빌리티 연동: 내 주변 은행 탐색 (LBS)
* **도입 배경**: 사용자가 숫자로만 구성된 예·적금 정보에서 한 걸음 더 나아가, 실제로 우대 통장 개설 등을 위해 방문해야 할 주변 오프라인 점포 위치를 직관적으로 파악할 수 있는 LBS(Location-Based Service)를 제공하고자 했습니다.
* **구조 설계**: 
  - 브라우저의 `Geolocation API`를 통해 획득한 기기 좌표(위/경도)를 카카오맵의 `coord2RegionCode` 객체로 역지오코딩하여 현재 위치를 인지하도록 설계했습니다. 
  - 특정 점포를 클릭할 경우, 단순한 Info 렌더링에 그치지 않고 즉각적인 **길 찾기(경로 폴리라인 드로잉) 및 예상 소요 시간 API**를 결합하여 모빌리티(Mobility)의 가치를 창출했습니다. 

### 2-2. 24시 금융 비서: 생성형 AI 챗봇 통합 (Gemini & RAG)
* **도입 배경**: 딱딱한 필터 검색 방식만으로는 본인에게 알맞은 상품을 찾기 주저하는 사용자들을 위해, 생성형 AI(Google Gemini API)를 결합하여 일상어로 실시간 1:1 재무 상담을 진행하는 챗봇 비서를 구축했습니다.
* **구조 설계**: 
  - LLM 모델 단독 연동 시 발생하는 허구 데이터 추천(Hallucination) 리스크를 줄이기 위해, 사용자 채팅 방 내에서 '상품', '추천' 등의 의도(Intent)가 감지되면 백엔드가 **직접 내부 DB의 금융상품을 1차 조건부 필터링(SQL)** 하도록 분기했습니다.
  - 필터링 된 실제 데이터들을 LLM System 프롬프트에 동기화(RAG 패턴 인젝션) 시킴으로써, 오차 없는 실가입 가능 금융상품만을 대답하도록 시스템을 안정화했습니다.

### 2-3. 금융상품 맞춤형 알고리즘: Heuristic Scoring
* **도입 배경**: 유저에게 금융상품을 추천할 때 백엔드(서버)에서 추천 알고리즘을 계산하면, 유저가 목표 기간 설정 슬라이더를 바꿀 때마다 과도한 N+1 Query 및 네트워크 통신 낭비가 일어납니다. 이를 해소할 경량화된 구조가 필요했습니다.
* **구조 설계**:
  - `BasicRecommend.vue` 컴포넌트 환경 안방에서, Vue 3의 핵심 훅인 `computed` 속성을 적극 응용하여 '클라이언트 측(Client-Side) 계산 알고리즘'을 설계했습니다. 
  - 사용자 프로필의 **연봉, 자산, 저축 성향(1~10)** 패러미터를 기초로 기준 자본 방어율을 산출한 뒤, 이자율 차이와 목표 기간의 차이를 계산하여 페널티 가중치를 실시간으로 차감하는 휴리스틱 스코어(Heuristic Score) 방식으로 슬라이드(Carousel)에 렌더링했습니다.

### 2-4. 금융결제원 연동 규격화 설계 (FIN MAP API)
* **도입 배경**: 내 주변 ATM 정보를 안정적으로 출력하기 위해, 단순 크롤링이 아닌 대한민국 금융 인프라의 표준인 금융결제원의 오픈 API(FIN MAP) 전용망을 구축하고자 했습니다.
* **구조 설계**: 
  - 무결성이 중요한 금융망 API 규격에 맞게 작동하기 위해, 프론트엔드가 아닌 백엔드(Django) 컨트롤러 계층 깊숙한 거점에 '트랜잭션 ID 발급 유틸리티(`generate_kftc_tran_id`)' 및 '사각형 Bounding Box 오프셋 생성기'를 캡슐화해 설계했습니다. 
  - 이로써 프론트 단과 외부망 사이의 의존성과 결합도를 완전히 낮추고 백엔드가 통신 관문(Gateway) 역할을 수행하도록 아키텍처를 잡았습니다.