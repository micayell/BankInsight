# BankInsight: 트러블슈팅 및 문제 해결 전략 (Troubleshooting)

프로젝트를 진행하며 직면했던 주요 기술적 한계와 버그들을 프론트엔드와 백엔드 영역으로 나누어, 어떠한 논리와 방식으로 해결했는지 기록한 문서입니다. 아키텍처 설계 관점에서의 고민과 사용자 경험(UX) 개선 과정을 담았습니다.

---

## 🎨 1. 프론트엔드 (Front-End) 트러블슈팅

### 💡 1-1. 고도화된 클라이언트 연산 기반 사용자 맞춤 알고리즘 (Heuristic Scoring)
* **🚨 문제 배경**: 단순히 최고 금리나 최저 금리 순서대로 나열하는 기초적인 추천은 고객의 '개인 재무 상태'와 '투자 성향'을 무시하는 한계가 있었습니다. 이를 해결하기 위해 백엔드(DB) 서버에서 추천 연산을 일일이 돌리기에는 유저 스케일이 커지거나 프로필 정보가 바뀔 때마다 극심한 N+1 통신 부하가 발생할 수 있다는 단점을 우려했습니다.
* **🛠️ 해결 과정 (BasicRecommend.vue 전략)**: 
  서버 부하를 덜고 클라이언트 단에서 즉각적인 연산이 가능하도록, Vue의 `computed` 속성을 응용한 **'휴리스틱 스코어링 알고리즘'**을 직접 프론트엔드 메모리 상에 설계했습니다.
  1. **예금/적금 목표 페널티 부과**: 사용자가 프로필에 입력한 저축 목표 기간(`desirePeriod`)과 실제 상품의 가입 옵션 기간을 대조하여, 기간이 1개월씩 어긋날 때마다 이자율 점수에서 0.15%p 씩 차감하는 '기간 거리 비례 페널티' 로직을 적용했습니다.
  2. **대출 변동성(Spread) 리스크 방어 가중치**: 유저의 자본력(연봉 + 자산 산술)을 산출하여 기준금액 대비 자본 방어율(`powerRatio`) 팩터를 추출했습니다. 유저의 자본 방어력이 낮고 투자 성향(`tendency`)이 안정적일수록, 대출 금리의 변동폭(최고 금리 - 최저 금리) 리스크에 강한 가중치(`riskPenaltyWeight`)를 곱하여 점수를 깎아내렸습니다. 결과적으로 유저의 상환 능력을 수학적으로 검증하여 가장 안전하고 저렴한 금리의 대출 상품을 완벽히 필터링(Sort) 해내는 데 성공했습니다.

### 💡 1-2. Virtual DOM 한계 극복을 위한 Optimistic UI 구현
* **🚨 문제 배경**: 카카오맵을 활용해 유저 위치 기반 은행 마커를 클릭하면 나타나는 '상세 팝업창(CustomOverlay)'에 '관심(찜) 버튼'을 개발했습니다. 이때 `CustomOverlay`는 컴포넌트가 아닌 **원시 HTML 문자열(Raw String)** 을 인자로 받기 때문에, Vue.js의 Virtual DOM을 우회하게 되어 `@click` 이벤트나 `v-model` 데이터 바인딩이 일절 동작하지 않는 렌더링 충돌 문제가 발생했습니다.
* **🛠️ 해결 과정**:
  1. **전역 이벤트 위임 (Global Event Handling)**: 원시 HTML 내부에 인라인 `onclick="window.toggleFavoriteBank(...)"` 속성을 연결하여, 전역 객체인 Window를 브릿지 삼아 내부 로직과 통신할 수 있게 우회했습니다.
  2. **낙관적 업데이트 (Optimistic UI)**: 인라인 이벤트 발생 시 클릭된 DOM 요소(`this`)를 파라미터로 넘겨받아 HTTP 응답을 기다리지 않고 `btnElement.innerText = '⭐'` 처럼 Vanilla JS 방식으로 버튼 스타일을 즉시 조작하여 사용자에게 딜레이 없는 피드백을 구현했습니다.

### 💡 1-3. 데이터 불일치: 외부 자원 의존성 한계 및 정규화
* **🚨 문제 배경**: 카카오의 Reverse-Geocoding API를 통해 넘겨받는 현재 위치 읍면동 데이터는 '전남', '광주'와 같이 축약된 과도기적 명칭을 반환합니다. 하지만 플랫폼 필터링 내부 DB는 최근 개편된 '전남광주통합특별시'로 유지되어, 양쪽 API의 데이터 불일치로 Select 드롭다운 자동 선택 로직이 깨지는 버그가 잦았습니다.
* **🛠️ 해결 과정**: 카카오의 응답을 컴포넌트에 다이렉트로 바인딩(Binding)하던 기존 방식을 폐기했습니다. 대신 Vue 컴포넌트 라이프사이클에 닿기 전 `normalizeSido()` 라는 브릿지 함수(미들웨어 역할)를 구축했습니다. 내부 매핑 규칙에 따라 호환 문구('광주' || '전남' -> '전남광주통합특별시')를 프로젝트 규격에 맞게 100% 포맷팅하는 전처리 파이프라인을 두어 외부 의존성 버그를 완벽히 해결했습니다. 

### 💡 1-4. 비동기 통신 UX 방어: 상태 시각화 및 중복 차단
* **🚨 문제 배경**: 사용자가 AI 챗봇에게 메시지를 전송하고 텍스트 스트리밍 또는 답변을 대기할 때, 외부 통신 지연 시 화면이 얼어버린 것처럼 보여 반복적으로 전송 버튼을 누르게 되는 UX적 악순환과 쓰레드 낭비가 존재했습니다.
* **🛠️ 해결 과정**: 전역 상태 Pinia에 `isLoading` 플래그를 두어, Axios 전송 시 `true`로 토글되게 구성했습니다. 해당 플래그를 감지하면 즉시 채팅 UI 하단에 CSS 바운스 애니메이션 기반의 **'타이핑 말풍선(...)'**을 그려냅니다. 이 시간 동안 Input 과 Button엔 완전히 `disabled` 속성을 걸어 실수로 인한 다중 전송 트래픽을 원천적으로 차단했습니다.

---

## ⚙️ 2. 백엔드 (Back-End) 트러블슈팅

### 💡 2-1. 금융결제원 FIN MAP 연동 한계점 및 Bounding Box 최적화 (KFTC API)
* **🚨 문제 배경**: 금융결제원 `FIN MAP API(openapi.finmap.or.kr)`를 통해 '내 주변 ATM/영업점' 정보를 가져오는 엔드포인트를 구현할 때, API 통신 규격이 매우 까다로웠습니다. 반경(거리)을 던지면 결과를 주는 카카오맵 방식과 달리, FIN MAP API는 반드시 사각형 형태의 위경도 경계값(`start/end_latitude`)인 **Bounding Box** 포맷을 강제하였고, 절대 중복되면 안 되는 20자리 고유 거래 번호(`api_tran_id`) 생성을 요구하여 잦은 `400 Bad Request` 에러로 연동이 실패했습니다.
* **🛠️ 해결 과정**:
  1. **Bounding Box 알고리즘 구현 (`get_bounding_box`)**: 프론트에서 넘어온 중심 위/경도 좌표에 대해 ±0.02도(대략 반경 2km 내외)의 오프셋을 수학적으로 더하고 빼어 직사각형 형태의 경계값을 동적으로 도출하는 연산 함수를 백엔드에 구축했습니다. 이로써 프론트 단은 복잡한 연산 없이 단순히 중심 좌표만 던지면 백엔드가 API 표준에 맞게 포맷을 자동 파싱하도록 결합도를 낮췄습니다.
  2. **고유 트랜잭션 ID 제너레이터 구현 (`generate_kftc_tran_id`)**: 수백 명이 접속해도 충돌(Collision)이 발생하지 않도록 시간 기반 문자열 렌더링에 만족하지 않고, `uuid4()`를 생성한 후 하이픈을 제거하여 정확히 20자리로 슬라이싱하는 전용 전처리 유틸리티를 설계했습니다. 결론적으로 금융권 수준의 엄격한 트랜잭션 식별 무결성을 달성하며 연동 성공률 100%를 보장했습니다.

### 💡 2-2. AI 할루시네이션(거짓 정보) 방지를 위한 RAG 파이프라인 구축
* **🚨 문제 배경**: 도입부에서는 사용자 메시지를 단순히 Google Gemini API에 던지는 역할만 했습니다. 그 결과 AI가 자체 학습 데이터 한계로 실존하지 않는 은행 상품이나 낡은 금리를 추천(할루시네이션)하는 등 심각한 오작동이 나타났습니다. 
* **🛠️ 해결 과정**:
  1. **자연어 인텐트(Intent) 사전 필터링 패턴**: Python 백엔드 내부에 `BANK_NAMES`와 같은 키워드 사전을 적용했습니다. 단순 직접 언급(`예/적금`)이 아니어도 은행 이름이나 `상품`, `추천` 키워드가 감지되면 조건문(ORM `Q` 객체)을 유동적으로 분기하여 관련된 은행 상품 DB를 선제적으로 조회하도록 구성했습니다.
  2. **검색 증강 생성(RAG, Retrieval-Augmented Generation) 도입**: DB에서 1차 추출된 실존 금융상품 및 옵션 객체들을 프롬프트 문자열로 직렬화하여, 사용자 질문과 함께 LLM의 `System Instruction(백그라운드 컨텍스트)`에 인젝션했습니다. 이로써 외부 AI 모델이 자체 학습망이 아닌 **우리가 주입한 검증된 DB 팩트 기반 값만을 사용하여 대답**하도록 완벽히 통제했습니다.
  3. **UX 퍼널을 위한 동적 라우팅 연계**: 조회했던 해당 상품 객체의 고유 식별자(`fin_prdt_cd`) 리스트를 HTTP 응답 JSON 구조의 `recommended_products` 배열로 추출해 반환했습니다. 프론트 단에서는 이를 감지해 채팅 버블 하단에 `[해당 상품 상세 페이지 이동]` 연동 버튼(딥링크)을 렌더링함으로써 전환율(Conversion) 모델의 마침표를 찍었습니다.

### 💡 2-3. 외부 서비스 장애 전파 차단 (Fault Tolerance 및 Fallback 설계)
* **🚨 문제 배경**: 외부망 기반의 Gemini AI 서버에 대규모 글로벌 트래픽 폭주 발생 시, 통신 지연(`Timeout`)이나 `503 Service Unavailable` 핑(Ping)이 수시로 반환되어 Django 서버 단까지 크래시 덤프 에러 형태가 사용자의 브라우저 측으로 노출되는 최악의 장애 전파 현상이 발생했습니다. 
* **🛠️ 해결 과정**: Django 애플리케이션 외부 API `Requests` 호출 로직에 철저한 `Try-Except` 분기 필터링망을 설치했습니다. HTTP 에러 코드 `429(Too Many Requests)`와 `503(Unavailable)`, `Timeout(504)` 등에 알맞게 맞춤형 폴백(Fallback) 조건을 Catch 하였습니다. 서비스 시스템이 통째로 멈추는 대신 *"현재 구글 서버 트래픽이 폭주하여 대기 중입니다. 잠시 뒤 재시도해주세요."* 와 같은 소프트하고 정제된 커스텀 JSON 예외 메시지로 치환해 반환함으로써 사용자 경험(UX) 최하단을 방어했습니다.